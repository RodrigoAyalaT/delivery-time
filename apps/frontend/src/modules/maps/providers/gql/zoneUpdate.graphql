mutation zoneUpdate($id: ID!, $name:String!, $color:String, $location:PolygonInput!){
    zoneUpdate(id: $id, input: {name: $name, color: $color, location: $location }){
        id
        name
        color
        location{
            type
            coordinates
        }
    }
}

