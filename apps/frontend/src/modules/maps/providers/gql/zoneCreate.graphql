mutation zoneCreate($name:String!, $color:String, $location:PolygonInput!){
    zoneCreate(input: {name: $name, color: $color, location: $location }){
        id
        name
        color
        location{
            type
            coordinates
        }
    }
}

