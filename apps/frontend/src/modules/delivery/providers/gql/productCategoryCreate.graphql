mutation productCategoryCreate($name:String!, $backgroundColor:String, $borderColor:String, $fontColor:String){
    productCategoryCreate(input: {name: $name, backgroundColor: $backgroundColor, borderColor: $borderColor, fontColor: $fontColor }){
        id
        name
        backgroundColor
        borderColor
        fontColor
    }
}

